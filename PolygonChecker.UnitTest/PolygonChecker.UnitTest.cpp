#include "pch.h"
#include "CppUnitTest.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace PolygonCheckerUnitTest
{
	TEST_CLASS(PolygonCheckerUnitTest)		// i didn't wanted to delete this in case if we would need it if we don't just delete it.
	{
	public:
		
		TEST_METHOD(TestMethod1)
		{
		}
	};
}
